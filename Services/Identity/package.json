{
    "type": "module",
    "scripts": {
        "start": "NODE_ENV=development && node ./src/server.js",
        "test": "NODE_ENV=test && NODE_OPTIONS=--experimental-vm-modules firebase emulators:exec --only firestore,auth \"jest\""
    },
    "dependencies": {
        "ajv": "^8.12.0",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "firebase": "^10.1.0",
        "firebase-admin": "^11.10.1",
        "firebase-functions": "^4.4.1"
    },
    "devDependencies": {
        "jest": "^29.6.4",
        "jest-extended": "^4.0.1",
        "jest-html-reporter": "^3.10.2",
        "supertest": "^6.3.3"
    }
}
